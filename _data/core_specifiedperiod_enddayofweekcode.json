
{
    "label": "endDayOfWeekCode",
    "uri": "core:SpecifiedPeriod.endDayOfWeekCode",
    "comment": "The code specifying the end day of the week for this specified period.",
    "domain": "core",
    "rangeIncludes": [
        {
            "uri": "schema:Text"
        }
    ],
    "domainIncludes": [
        {
            "uri": "core:SpecifiedPeriod",
            "comment": "A specified period of time."
        }
    ]
}