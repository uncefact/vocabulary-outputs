
{
    "label": "encodingCode",
    "uri": "core:BinaryFile.encodingCode",
    "comment": "The code specifying the encoding of this specified binary file.",
    "domain": "core",
    "rangeIncludes": [
        {
            "uri": "schema:Text"
        }
    ],
    "domainIncludes": [
        {
            "uri": "core:BinaryFile",
            "comment": "A specified computer file or program stored in a binary format."
        }
    ]
}