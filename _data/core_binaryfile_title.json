
{
    "label": "title",
    "uri": "core:BinaryFile.title",
    "comment": "A title, expressed as text, for this specified binary file.",
    "domain": "core",
    "rangeIncludes": [
        {
            "uri": "schema:Text"
        }
    ],
    "domainIncludes": [
        {
            "uri": "core:BinaryFile",
            "comment": "A specified computer file or program stored in a binary format."
        }
    ]
}