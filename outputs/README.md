# vocabulary-outputs
The repository for intermidiate publishing of vocaulary outputs in jsonld and html representation

For HTML presentation Jekyll project will be setup to use adopted JSON files created based on JSON-LD vocabularies. 
The intermideate JSON files are needed to separate HTML generation from vocabulry files parsing.

The HTML outputs from Jekyll build will be uploaded to AWS S3 along with JSON-LD files and served via AWS CloudFront.
